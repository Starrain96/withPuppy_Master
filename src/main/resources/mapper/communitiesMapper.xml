<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="communities">
	<!-- insert, delete, update는 무조건 resultType="int"이므로
	     프로그래머가 써주지 않아도 이미 resultType="int"가 
	     있는 것으로 본다.!
	 -->
	<insert id="create" parameterType="communitiesVO">
		insert into communities (commu_id, commu_no,commu_title,commu_content)
		values (#{commu_id}, null , #{commu_title}, #{commu_content})
	</insert>
	
	<delete id="del"  parameterType="Int">
		delete from communities where commu_no = #{commu_no}
	</delete>
	
	<update id="up"  parameterType="communitiesVO">
		update communities set commu_content = #{commu_content} where commu_no = #{commu_no}
	</update>
	<select id="one" parameterType="communitiesVO" resultType="communitiesVO">
		select* 
		from communities
		where commu_no = #{commu_no}
	</select>
	<select id="all" resultType="communitiesVO">
		select*
		from communities
	</select>
</mapper>



